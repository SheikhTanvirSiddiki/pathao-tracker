services:
  # Python Flask API Server (Simplified)
  - type: web
    name: pathao-tracker-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn server_simple:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
    healthCheckPath: /
    region: oregon
