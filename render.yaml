services:
  # Python Flask API Server (Real Pathao Scraper)
  - type: web
    name: pathao-tracker-api
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    region: oregon
